[project]
name = "miam-loadtests"
version = "0.1.0"
description = "Load tests for Miam recipe API"
requires-python = ">=3.13"
dependencies = [
    "locust>=2.32.0",
]

[tool.hatch.build.targets.wheel]
packages = ["helpers"]
